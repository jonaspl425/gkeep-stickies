{
  "name": "sticky-notes-desktop",
  "version": "1.0.0",
  "private": true,
  "description": "Persistent desktop sticky notes with Google Keep account sync and import/export.",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "test": "node --test"
  },
  "devDependencies": {
    "electron": "^35.0.0"
  }
}
